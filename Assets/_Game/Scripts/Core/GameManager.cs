using UnityEngine;

namespace Stonehold
{
    /// <summary>
    /// Central coordinator for a run. Holds the current GameState and connects the
    /// managers (Wave, Tower, Economy, Enemy, Unlock). Scaffolding only — no logic yet.
    /// </summary>
    public class GameManager : MonoBehaviour
    {
    }
}
