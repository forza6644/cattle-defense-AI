namespace Stonehold
{
    /// <summary>
    /// Player-facing run difficulty. Heat mutators remain an internal ruleset.
    /// Nightmare is reserved for a later expansion and is not selectable yet.
    /// </summary>
    public enum DifficultyMode
    {
        Normal = 0,
        Hard = 1,
        Nightmare = 2
    }
}
