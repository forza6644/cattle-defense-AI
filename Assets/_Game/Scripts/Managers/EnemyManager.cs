using UnityEngine;

namespace Stonehold
{
    /// <summary>
    /// Keeps track of the currently alive enemies and reports when a wave is cleared.
    /// Scaffolding only — no logic yet.
    /// </summary>
    public class EnemyManager : MonoBehaviour
    {
    }
}
